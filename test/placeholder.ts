// Test suite for signal-appservice-discord
// Add tests here as the project grows.
// Run with: npm test

import { expect } from "chai";

describe("Bridge", () => {
    it("should load configuration", () => {
        expect(true).to.equal(true);
    });
});
